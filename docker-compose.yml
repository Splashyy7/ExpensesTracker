services:
  expenses-tracker:
    build: .
    image: expenses-tracker:latest
    container_name: expenses-tracker
    stdin_open: true
    tty: true
    environment:
      EXPENSES_DATA_FILE: /data/despesas.json
    volumes:
      - expenses-data:/data

volumes:
  expenses-data:
    driver: local
